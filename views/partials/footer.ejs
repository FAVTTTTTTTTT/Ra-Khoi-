<!-- footer.html -->
  <style>
       body {
    background: linear-gradient(135deg, #e0f7fa 0%, #fffbe7 100%);
    font-family: 'Roboto', Arial, sans-serif;
    font-size: 0.5rem;
    color: #222;
}

        .navbar {
            background: linear-gradient(90deg, #0093e9 0%, #80d0c7 100%) !important;
            box-shadow: 0 2px 12px rgba(0, 147, 233, 0.08);
            font-family: 'Montserrat', Arial, sans-serif;
        }

        .navbar-brand,
        .nav-link,
        .btn-order {
            color: #fff !important;
        }

        .navbar-brand img {
            border: 2px solid #fff;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        }

        .navbar-brand span {
            font-family: 'Montserrat', Arial, sans-serif;
            font-weight: 900;
            font-size: 2rem;
            letter-spacing: 1px;
            text-shadow: 1px 2px 8px #0093e9a0;
        }

        .navbar-nav .nav-link {
            font-weight: 600;
            margin-right: 10px;
            transition: color 0.2s;
            font-size: 1.08rem;
        }

        .navbar-nav .nav-link:hover,
        .navbar-nav .nav-link.active {
            color: #ffd700 !important;
        }

        .btn-order {
            background: #ffd700;
            color: #0093e9 !important;
            font-family: 'Montserrat', Arial, sans-serif;
            font-weight: 800;
            border-radius: 24px;
            padding: 8px 24px;
            border: 2px solid #0093e9;
            transition: background 0.2s, color 0.2s, box-shadow 0.2s;
            box-shadow: 0 2px 8px rgba(0, 147, 233, 0.08);
            letter-spacing: 0.5px;
        }

        .btn-order:hover {
            background: #0093e9;
            color: #fff !important;
            box-shadow: 0 4px 16px rgba(0, 147, 233, 0.15);
        }

        .carousel-inner {
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 8px 32px rgba(0, 147, 233, 0.15);
        }

        .carousel-item img {
            height: 420px;
            object-fit: cover;
            transition: transform 0.7s cubic-bezier(.4, 2, .3, 1), filter 0.7s cubic-bezier(.4, 2, .3, 1);
            filter: brightness(0.98) saturate(1.1) hue-rotate(-8deg);
        }

        .carousel-item.active img,
        .carousel-item-next img,
        .carousel-item-prev img {
            transform: scale(1.15);
            filter: brightness(0.95) saturate(1.15) hue-rotate(-8deg);
        }

        .carousel-indicators [data-bs-target] {
            background-color: #0093e9;
            width: 14px;
            height: 14px;
            border-radius: 50%;
        }

        .section-title {
            color: #0093e9;
            font-family: 'Montserrat', Arial, sans-serif;
            font-weight: 900;
            margin-bottom: 32px;
            letter-spacing: 1px;
            font-size: 2.3rem;
            text-transform: uppercase;
            text-shadow: 1px 2px 8px #80d0c7a0;
        }

        .product-card img {
            height: 220px;
            object-fit: cover;
            border-radius: 16px 16px 0 0;
            border-bottom: 4px solid #ffd700;
        }

        .product-card {
            border: none;
            border-radius: 16px;
            overflow: hidden;
            transition: box-shadow 0.2s, transform 0.2s;
            background: linear-gradient(135deg, #fffbe7 60%, #e0f7fa 100%);
            box-shadow: 0 2px 12px #0093e91a;
        }

        .product-card:hover {
            box-shadow: 0 8px 32px #0093e933;
            transform: translateY(-8px) scale(1.03);
        }

        .product-card .card-title {
            color: #0093e9;
            font-family: 'Montserrat', Arial, sans-serif;
            font-weight: 800;
            font-size: 1.25rem;
            letter-spacing: 0.5px;
        }

        .product-card .card-text {
            font-family: 'Roboto', Arial, sans-serif;
            font-size: 1.05rem;
            color: #444;
        }

        .product-card .btn-order {
            margin-top: 10px;
            width: 100%;
            background: #0093e9;
            color: #fff !important;
            border: none;
            font-weight: 700;
        }

        .product-card .btn-order:hover {
            background: #ffd700;
            color: #0093e9 !important;
        }

        .footer {
            background: linear-gradient(90deg, #0093e9 0%, #80d0c7 100%);
            color: #fff;
            padding: 30px 0 10px 0;
            margin-top: 60px;
            border-radius: 24px 24px 0 0;
            font-family: 'Montserrat', Arial, sans-serif;
            font-weight: 700;
            font-size: 1.08rem;
            letter-spacing: 0.5px;
        }

        .footer .list-inline-item:not(:last-child)::after {
            content: "|";
            color: #fff;
            margin: 0 12px;
            opacity: 0.5;
        }

        .footer a {
            color: #fff;
            font-weight: 700;
            text-decoration: none;
            transition: color 0.2s;
        }

        .footer a:hover {
            color: #ffd700;
        }
    </style>
<footer class="footer mt-5">
    <div class="container text-center">
        <img src="/images/logo.jpg" alt="Logo" width="50" height="50" class="mb-2 rounded-circle">
        <p class="mb-1">© 2025 Ra Khoi Restaurant. All rights reserved.</p>
        <p class="mb-0">Hotline: 0901797969 | Địa chỉ: 131 BIS KHU PHỐ 1 TT. DƯƠNG ĐÔNG, HUYỆN PHÚ QUỐC, TỈNH KIÊN GIANG</p>
    </div>
</footer>
